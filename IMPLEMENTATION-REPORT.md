# Отчёт по реализации Среза

Дата работы: 15.09.2026. Фиксированная дата данных: 14.09.2026, 10:00, Москва.

## Итог и границы приёмки

Создан отдельный проект `srez-codex` с работающими frontend/backend, исходниками, lockfile, пятью оригинальными предметными ассетами, тремя темами, общей моделью данных и тестами. Приложение запущено локально на **127.0.0.1:5178**; сервер на **127.0.0.1:4178**. HTTP-запуск подтверждён; тесты и сборка выполнены.

Обновление: встроенный браузер доступен, выполнены ручные сценарии на ширинах 320/390/768/1280 px. Подробности и ограничения: `PERFORMANCE.md`. Live API без серверного ключа не проверен.

## Стек и запуск

React 19.1.1, TypeScript 5.9.2, Vite 7.1.4, React Router 7.8.2, Motion 12.23.12, Zustand 5.0.8, TanStack Table 8.21.3, Lucide, Inter с кириллицей. Backend: Node 24.19.0 в проверенной среде, Express 5.1.0, встроенный SQLite, OpenAI SDK 6.2.0, Zod, Sharp, PDF.js, локальные файлы. Графики — общие SVG; PNG — html-to-image; QR — qrcode.

```sh
pnpm install --frozen-lockfile
pnpm dev
pnpm test
pnpm build
```

`npm run dev` также является единым запуском обоих процессов после установки. Подробности подключения AI и production-запуска — в README.

Чистая установка по lockfile проверена в отдельной папке `/private/tmp/srez-clean-install-0915`: 247 пакетов, успешный exit code 0. Первоначальная offline-попытка не нашла пакет в локальном кэше; установка с разрешённым доступом к registry завершилась успешно.

## Реализованные разделы

| Раздел | Что поставлено |
|---|---|
| Обзор | Открытые KPI, общий период/магазины в URL, сравнение, тёмный SVG-график, метрики, tooltip, выделение диапазона, таблица значений, возможность, магазины, категории, CSV, вопрос помощнику |
| Продажи | Динамика, 12 магазинов, товары/SKU, реальные сочетания категорий, временная теплокарта, фильтры и таблицы |
| Покупатели | Типизированное дерево И/ИЛИ/НЕ, редактирование/порядок условий, временные окна, сохранение/копирование/удаление сегмента, исключения, профиль, связанные чеки |
| Кампании | Общие карточки и таблица, поиск/статусы, черновики, копии, архив, пять шагов, проверка экономики и бюджета, журнал действий |
| Студия | Общий CouponRenderer, три композиции, три темы, редактируемые нативные слои, crop/размер/выравнивание, история, варианты, избранное, JSON/PNG, мобильный preview |
| Стиль сети | Серверная загрузка изображений/безопасного SVG/PDF/шрифтов, выбор страниц, версии, provenance, редактирование профиля, утверждение, preview применения |
| Симуляция | Стабильные группы, snapshot правил/дизайна, будущая дата, касса с корзиной, отказы по условиям, идемпотентность, одноразовость, возврат строк |
| Результаты | CP-097 как отдельный fixture, формула 19 200 ₽, интервал с нулём и корректный вывод; отдельные результаты событий симуляции |
| Помощник | 12 локальных намерений, общие расчёты, контекст, переходы, сохранённая история, повтор/копирование |
| Данные | Настоящий CSV, шаблон, предпросмотр, ошибки/конфликты/повтор, отдельный набор, сценарии качества |
| Настройки | Имя/магазин, интенсивность движения, роли, статусы подключения, сбросы, тур и поиск Cmd/Ctrl+K |

## Данные

12 магазинов; 16 000 профилей; 192 товара; четыре категории; 110 873 операции. Генератор детерминирован и запускается в Web Worker, затем хранится в IndexedDB. Контрольные суммы в README и автоматическом выводе тестов.

Аудитория CP-104: 1 854. Исключения 333 + 167 + 123. Доступно 1 231: предложение 615, контроль 616. Максимум скидок 30 750 ₽. Карточка и назначение используют одинаковое округление.

## Автоматические проверки

**35 тестов проходят, 0 ошибок.** Дополнительно проверены графики, периоды, кэши, сохранение и округление аудитории.

| Область | Подтверждённые проверки |
|---|---|
| Seed | Размер, стабильность повторной генерации, ссылки, анонимные чеки, возвраты |
| Агрегации | Сходимость дней/магазинов/категорий с сетью, сетевые уникальные ID, влияние магазина |
| Экономика | 190 / 115 / 10% → 171 / 56 / 32,75%; 25% → 142,50 / 27,50 / 19,30%; блокировки маржи и бюджета |
| Деньги | Распределение скидки по строкам с точным остатком в копейках |
| Сегменты | И/НЕ, пустые условия, null-возраст, недостаточная история, последовательные исключения, отсутствие канала |
| Назначения | Детерминированность и отсутствие пересечения предложения/контроля |
| Даты | Московские границы, два интервала CP-104, дни/часы погашения |
| Касса | Успех, неверный магазин/дата/контроль, повтор operationId, отказ другой операции после использования |
| Возврат | Связь с исходным чеком, нулевые итоговые выручка/ВП после возврата, повтор возврата отклонён |
| Неполные данные | ВП только по известной части, реальное покрытие стоимости |
| CSV | Корректный импорт, повтор без удвоения, конфликт и неизвестный товар |
| Помощник | 12 намерений и отказ неизвестному вопросу |
| Рендер | Основные страницы рендерятся без ошибок JSX; три формата содержат одинаковые сроки и не раскрывают токен |
| Backend | Отсутствие ключа, сохранение дизайна/CAS-конфликт, запрет изменения скидки через дизайн |
| Jobs | Идемпотентность, сохранение байтов, ветка правки и edit target, частичный результат, отмена, interrupted после рестарта |
| Brand assets | Чужой asset/variant отклонён; активный SVG отклонён; PNG сохранён; PDF преобразован в выбранную страницу |
| Провайдер | Реальный SDK-адаптер вызывает generate/edit через подменённый тестовый транспорт; автоматические повторы отключены |

Серверный рендер не проверяет работу CSS, реальные клики, focus и анимации. Не указан как E2E или визуальная приёмка.

## Статус AI отдельно

| Операция | Реализация | Контрактный тест | Live |
|---|---|---|---|
| Анализ стиля | Responses API, изображения и структурированная схема, `gpt-4.1` | Пройден: job, собственные референсы, draft → confirmed | Не выполнен: нет серверного ключа |
| Генерация | Image API `images.generate` / reference-conditioned `images.edit`, `gpt-image-2.5-sunburst` | Пройден: байты/ID, 1/3 варианта, идемпотентность, ошибки | Не выполнена: нет серверного ключа |
| Текстовая правка | `images.edit` с исходным вариантом и референсами, новая ветка | Пройден: исходный asset первым, parentVariantId, исходник остаётся | Не выполнена: нет серверного ключа |

Фактический GET `/api/status`: `connected=false`, `analysisConnected=false`, `mode=example`. В frontend нет API-ключа. Генерация примеров встроенным ImageGen во время разработки отдельно отмечена в `public/assets/provenance.json` и не считается live-проверкой приложения.

## Визуал и скриншоты

Все четыре исходных референса открыты и изучены до построения интерфейса. Реализованы общий sidebar 224 px, светлая рабочая область, графитовый график, киноварный акцент, карточная сетка, встроенный профиль и студия с крупным купоном. Отдельно созданы и просмотрены пять предметных сцен, без запечённых UI-надписей.

Реальные снимки показаны в рабочей беседе. Локальные PNG-файлы экранов отдельно не экспортировались. Мобильные размеры проверены через DOM-viewport локальной iframe-оболочки.

Подготовлен `e2e/app.spec.ts`: фильтр → CSV, правило сегмента → профиль/чек, кампания → студия → сохранение/reload, маржа → запуск → касса → повторный отказ; снимки четырёх экранов и брейкпоинтов. Этот файл поставлен, но его выполнение не заявляется. Инструкция — `screenshots/README.md`.

## Что остаётся проверить

- Полный автоматический E2E-проход и все варианты пользовательских загрузок. Ручной проход описан в `PERFORMANCE.md`.
- Анализ → генерация → текстовая правка с реальным серверным API-ключом и доступом к выбранным моделям.
- Фактическое время реакции интерфейса и частота кадров. Тесты измеряют выполнение кода Node, но не доказывают 60 fps в браузере.
- WebMCP предусмотрен с feature detection; регистрация/вызовы в поддерживающем браузере не проверены.

Операционные ограничения концепта: локальные роли без production-защиты; нет кассовой интеграции/рассылок/платежей; стоимость внешнего API не оценивалась выдуманными тарифами. Bootstrap-интервал CP-097 взят из заданного fixture, а не перенесён на новую аудиторию. Свойства и результат реализации Claude не исследовались и не оцениваются.
